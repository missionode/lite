# CP-WF-001 — Efficient Loop workflow

## START / specification

User approved encoding the preceding efficiency recommendations into Loop so they are followed. Target: this repository's `Loop/` workflow and `AGENTS.md`. Reusable reference: installed Loop 0.3.1 skill; do not change its cache or global configuration.

Acceptance: a concise companion reachable from project and Loop entry points covers bounded context, advisory versus actual routing, deterministic automation, behavior tests, measured debugging, incremental modularity and honest usage reporting. Safety, browser opt-in, source/atlas fidelity and external approval boundaries remain intact.

Excluded: automatic child launches, app refactors, new release automation, telemetry, history archival, dependency installs, plugin reinstallation or production push. These are workflow requirements/future scoped tasks, not delivered app features.

## MID / plan and baseline

- Baseline: production 361938c; only private `.codex/` and backup audio were untracked.
- Environment: Darwin arm64, Node v25.9.0, Python 3.9.6; no network, services or new dependencies needed for this policy change.
- Read installed root policy, delivery and routing contracts; inspected local policy differences and current handoff/source structure.
- Add one reusable companion and entry links; add static wiring checks and pressure scenarios; run local contract/router tests; check diff and preserve unrelated files.
- Execution mode: direct; this documentation task does not benefit from a second model repeating the work. No model switch or savings claimed.

## NOW / review and verification

- SPEC_COMPLIANCE: all seven workflow areas covered; project-local enforcement linked from AGENTS and local Loop skill; no application behavior changes.
- QUALITY_AND_RISK: explicit approvals remain; mandatory instructions cannot be skipped; expected failures cannot be silently hidden; no budget or quota claims; no plugin-cache mutation. Installed plugin has not been upgraded.
- COMPLETE within policy-maintenance scope: `python3 -m unittest Loop/tests/test_efficiency_contract.py Loop/tests/test_codex_model_router.py` passed 12 checks; `git diff --check` passed. Command stderr contains only the normal unittest summary, no unexpected errors. Host pressure scenarios are documented for future evaluation, NOT_RUN here; static checks cannot establish real model compliance or token savings.
- Atlas remains accurate for executable app behavior; no diagram change needed for instruction-only work.
- Next: use this companion for the next bounded task. Implement scripts, archive restructuring or module extraction only as separately scoped work.
