# Plan

1. Add the modularization safety-loop map and queue future boundaries.
2. Warm-up: extract settings backup helpers and test the module directly.
3. Centralize settings schema and persistent/session state ownership. Complete: initial-state factory and direct behavior contract delivered.
4. Extract content and localization services. Complete: lookup, fallback, localized shapes and script validation. Timing controls remain with UI until their ownership boundary.
5. Separate narration and audio lifecycle owners. Partially complete: deterministic media primitives delivered in CP-MOD-005, Piper lifecycle in CP-MOD-006, and shared effect-route retirement in CP-MOD-007. AudioEngine bus construction is explicitly deferred until the next weekly reset.
6. Convert journey orchestration into reusable stage contracts. In progress: CP-MOD-008 owns deterministic focused-mode selection, launch priority, prelude-safe chakra validation and chakra-order selection; CP-MOD-009 owns the ordered preparation-stage plan; CP-MOD-011 owns sequential execution and the between-stage active-session guard; CP-MOD-012 through CP-MOD-018 own the Body Scan, Guided Noting, Dharana, Box Breathing, Visualization, Ho’oponopono and Undo & Unlearn practice lifecycles. CP-MOD-019 owns shared screen navigation; CP-MOD-020 owns Lobby session estimates.
7. Split Lobby, Settings, Advanced Features and session UI controllers. CP-MOD-021 extracts the Mood & Relaxation ambience settings renderer while preserving the Advanced Features gate and recovery affordances. CP-MOD-022 extracts drone-duration radio/note synchronization; app retains all timing calculations and selection persistence. CP-MOD-023 extracts the Lobby experience-visibility controller with app-injected localization, state, timing and supporting UI refresh callbacks. CP-MOD-024 extracts Yoga setup selection persistence and timing-row visibility, while preference hydration and Yoga session execution remain app-owned. CP-MOD-025 extracts range formatting, step-button enhancement and display refresh; timing configuration and business state remain app-owned. CP-MOD-026 extracts localized Lobby roadmap resolution and rendering; journey validation/dispatch remain app-owned. CP-MOD-027 extracts the shared display-language UI renderer while app retains sky refresh and invokes roadmap/duration summary refreshes in the same order. CP-MOD-028 extracts timing-config fallback/profile resolution, control bounds, preference defaults/clamping and loading; app retains timing accessors and demo-specific application.
8. Introduce measured feature-level lazy loading only after extraction parity:
   - Capture startup script/heap cost, journey-start latency, active worker/audio/animation resources and offline behavior before changing delivery.
   - Keep the essential shell eager: state/settings, localization, Lobby controls, journey routing and core lifecycle safety.
   - Define cohesive optional bundles for candidates such as Advanced/Yoga/Intimate/Experiments, Sleep, video introduction, specialized preparation practices, Piper runtime/model and optional spatial effects. Confirm each boundary from measurements rather than splitting every small file.
   - Convert approved optional boundaries to dynamic loading. Let the service worker cache bytes for offline use without executing them at startup; deduplicate concurrent imports and surface a safe localized failure path. CP-MOD-038 applies this to seven selectable guided-practice modules: Begin loads only checked practices before optional video/audio startup, and a missing module returns the user to a retryable Lobby state.
   - Begin preload when the user selects or approaches an optional experience, then let Begin await only unfinished preparation. Do not preload unselected features or create background polling.
   - Add explicit release contracts for workers, decoded audio buffers, AudioNodes, media element sources, animation frames, WebGL/canvas owners, observers, timers and listeners where safe.
   - Revalidate cold/warm/offline startup, every journey route, cancellation/restart, PWA update behavior and journey-start latency. Adopt each lazy boundary only when measured memory/CPU/startup benefit outweighs added complexity.

Every phase stops on test failure, overlapping external changes or behavior disagreement.

CP-MOD-013 extracts Guided Noting's timed reminder loop, cancellation checks and black-scene cleanup behind a frozen directly tested API. The app remains responsible for selecting localized copy and injecting existing screen, visual, narration, sleep and session services; eager loading and offline precache remain unchanged. This is an ownership/testability step, not a performance optimization. Continue with the remaining self-contained practice lifecycles and UI controllers before measuring optional loading boundaries.

CP-MOD-014 extracts Dharana's anchor selection, shrinking session clock, narrated four-second release and cleanup behind a frozen direct-test API. CSS still supplies the same anchor/veil transitions, while session time continues to shrink the anchor when reduced motion disables animation. No narration order, duration, visuals or audio behavior should change.

CP-MOD-015 extracts Box Breathing's tutorial, localized four-step cycle, responsive pause accounting, completion narration and background-music handoff behind a frozen direct-test API. The controller supplies existing translation, timing, audio, narration and session services. The established 100 ms pause-check cadence is intentionally unchanged; this extraction is not a CPU optimization.

CP-MOD-016 extracts Visualization's blackout, narration, silence wake prompt, ambient/music handoffs, active-duration clock, gradual re-entry and cleanup sequence behind a frozen direct-test API. The controller injects localized narration and existing audio, screen and session services. Preserve current ordering, fade durations and cancellation behavior; this is an ownership/testing step, not a performance optimization.

CP-MOD-017 extracts Ho’oponopono's localized opening, three cycles of the four phrases, configured pauses, closing fade handoff and visual setup behind a frozen direct-test API. The app continues to own integration-stage placement, script localization, audio/narration and session timing services; preserve the current order and behavior.

CP-MOD-018 extracts Undo & Unlearn's black-scene setup, localized phase sequencing, active-session guards and fade cleanup behind a frozen direct-test API. Preserve its content-free private-reflection contract and existing timing. Guarantee DOM/body cleanup even if the scene-release delay rejects; leave integration placement, copy and session services in the app.

CP-MOD-019 extracts the shared screen-visibility/static-sky/scroll-reset behavior into an injectable navigation owner. Preserve all screen IDs, scroll resets, decorationchange notification, and the dynamic sky on Lobby and Settings. Existing call sites remain routed through the app's compatibility wrapper.

CP-MOD-020 extracts the ordered Lobby session-duration estimate calculation into a directly tested view-model. Preserve every focused-mode priority, default duration, translation key, timing-config input, demo adjustment, and selected-chakra/add-on formula. App retains the rendering and roadmap refresh.

CP-MOD-021 extracts rendering/synchronization of the Mood & Relaxation ambience controls from the Settings/Lobby view logic into a frozen UI module. Preserve the Advanced Features visibility gate, session-only selection, No Frequency disabling, missing-source recovery message and all existing slider formatting. This is eager-loaded and precached ownership work, not a performance optimization.

CP-MOD-022 extracts only drone-duration option and contextual-note synchronization. Preserve app-owned mode selection, HRIM's Beginner restriction, Sleep/standard eligibility and the existing duration summary calculation. This is eager-loaded and precached ownership work, not a performance optimization.

CP-MOD-023 extracts Lobby experience visibility and associated display synchronization behind a frozen controller API. Preserve selected-mode guards, standalone preparation behavior, Advanced Features/No Frequency Shot restrictions, option visibility, duration range values and labels, Shot/Intimate Service/Yoga presentations and refresh order. The app injects live state, localization, timing and adjacent controller callbacks. Eager delivery remains; no performance benefit is claimed.

CP-MOD-024 extracts the Yoga Experience settings boundary: reading checked Corpse Pose/Bath/selected-pose controls, persisting those values immediately and on Settings save, and synchronizing the related timing-row visibility/accessibility state. The app still hydrates persistent state and consumes the Yoga selection during session validation/execution. Preserve the existing storage keys and visible-row layout defaults. Eager delivery remains; this is an ownership extraction, not a performance optimization.

CP-MOD-025 extracts the shared range-control UI: value formatting, idempotent DOM enhancement, bounded increment/decrement buttons, disabled boundary states and display refresh. The app remains responsible for timing-config interpretation, state updates and value persistence. Preserve the current labels, rounding, time units, DOM structure, event bubbling, boundaries and offline script order. Eager delivery remains; this is not a performance optimization.

CP-MOD-026 extracts the display-only Lobby journey roadmap. Preserve exclusive-mode priority, standalone-add-on fallback, exact preparation order, optional video prefix, translated labels and the ` » ` separator. App retains the compatibility wrappers and all Begin validation/dispatch behavior. Eager delivery remains; this is not a performance optimization.

CP-MOD-027 extracts display-language DOM rendering for titles, labels, placeholders, ARIA labels, grouped options and selectable-control labels. Preserve stale-locale HTML fallback, renderer call order, dynamic sky invalidation in the app, and the app-owned roadmap/duration summary refresh callbacks. Eager delivery remains; this is not a performance optimization.

CP-MOD-028 extracts timing-config fallback lookup, shallow named-profile overlays, range-bound application, existing localStorage default/clamp behavior and config-fetch failure fallback. App retains compatibility accessors and the demo core-duration preset callback. Preserve section-priority, units, storage keys, rounding and order. Eager delivery remains; this is not a performance optimization.

CP-MOD-029 extracts automatic journey voice-profile selection/application. Preserve the High Energy balanced preset, the non-high-energy Shringara profile only for a feminine narration voice, and the standard soft profile otherwise. The app retains voice-gender/registry detection and supplies live state, localStorage, UI, document and audio services. Preserve existing tuning values, storage keys, spatial/music echo synchronization and call ordering. Eager delivery remains; this is not a performance optimization.

CP-MOD-030 extracts page-load reset of session-only Box Breathing, Ho'oponopono, Music Only, High Energy and Sleep selections plus cleanup of their retired localStorage keys. Preserve exact ordering around persisted controls and existing Yoga setup restoration; Yoga mode/legacy-key handling remains app-owned in this slice. Preserve all storage keys, checkbox ids and reset defaults. Eager delivery remains; this is not a performance optimization.

CP-MOD-031 extracts the ordered mixer/preference control-value synchronization from loadPreferences. Keep the app as state owner and inject its existing syncValue helper. Preserve all 19 control/value pairs, duplicate Lobby/Settings mirrors and relative placement after drone-duration setup and before session statistics/chakra selections. Eager delivery remains; this is not a performance optimization.

CP-MOD-032 extracts hydration of selected chakras, the defaulted intention and the returning/video/audio-filter toggles. Keep selection state, default intention policy, validation and journey start in the app; inject current state and existing UI helpers. Preserve selection order, fallback behavior and position before frequency/session-mode controls. Eager delivery remains; this is not a performance optimization.

CP-MOD-033 extracts core and auxiliary timing-control hydration: core duration range fills/labels and the ordered journey-duration values/labels. Preserve existing slider fill percentages, minute/second formatting, order, timing defaults, duration calculations and drone-mode callbacks. App retains config, state and estimate owners. Eager delivery remains; this is not a performance optimization.

CP-MOD-034 extracts the existing visual-effect selector/application and brightness-control/CSS synchronization from preference hydration. Preserve the call order around Yoga, journey timing and script selection; app retains visual state and rendering services. Eager delivery remains; this is not a performance optimization.

CP-MOD-035 extracts saved script-source control hydration, custom-script panel visibility and the existing status message. App retains script state, demo selection/timing decisions, loading, validation and voice selection. Preserve call order before range-control refresh. Eager delivery remains; this is not a performance optimization.

CP-MOD-036 extracts the three persisted personal-care checkbox restorations (perineal care, assisted bathing and massage) into one directly tested presentation boundary. Preserve mapping and setup order after the persisted Bath control; app retains state, session gating and care execution. Eager delivery remains; this is not a performance optimization.

CP-MOD-037 fixes offline shell cache parity for the versioned app.js and style.css URLs and adds a repeatable Chromium cold/warm/offline baseline that verifies service-worker control, exact cache entries, resource transfer and page errors without starting playback. Local static-server measurements are indicative only: the seven optional preparation-practice scripts total about 16.9 KiB of about 903 KiB encoded JavaScript (roughly 1.9% of initial JavaScript); this supports a modest byte-saving experiment, not a claim of material CPU, thermal or device improvement. Browser checks must prove only selected practice code loads, standalone routes work, and selected modules load offline before adopting the boundary.

CP-MOD-038 adopts the smallest approved code-splitting boundary: a tiny eager manifest/loader plus seven selected practice scripts. The selected toggles are resolved on Begin, scripts load concurrently once before the video/audio journey prelude, and each practice method independently ensures its module is available for direct routes. The service worker keeps the exact module URLs precached. Unselected practice APIs are not parsed/executed; a missing module clears its failed promise and presents localized retry guidance in English, Malayalam, Hindi and Russian. Initial local JavaScript body bytes fell about 13.1 KiB (~1.4%) in a single cold sample (from ~903.4 to ~890.3 KiB); timing variation remains high and no CPU, thermal, device or production benefit is claimed. Preserve both cached selected/offline route parity and no-unselected-load behavior in further boundaries.

CP-MOD-039 defers only the optional generated video media: retain the small image and journey-chrome handlers, but set no initial media URL and no preload. Attach the URL only at explicit video preview or after the user opts into the Lobby introduction and presses Begin. The clip stays outside PWA precache; preserve the existing safe unavailable-video journey fallback offline. Acceptance: browser request evidence proves zero video requests on ordinary initial load and one request only after opt-in; preview still attaches the same media URL; the app/cache version updates; no flow, video timing, audio, narration, or sky behavior changes. Quantify only avoided unnecessary media requests/bytes; do not claim thermal or device gains without matching measurement.

CP-MOD-040 moves fullscreen journey chrome out of the optional-video controller and into one app-lifetime `JourneyChromeController`. Preserve pointer/touch/pen/keyboard behavior, 180 ms hover hide, 3 s idle cursor/touch reveal, mixer keep-visible semantics, fullscreen tracking, page-visibility cleanup and no-animation-loop behavior. Load its small script before `app.js` and precache it offline. The app owns one shared instance independently of `JourneyVideoPrelude`. No user-visible or performance claim; use the existing direct timer/interaction contract against the extracted module, app-wiring/cache assertions and targeted journey/video/sleep regressions.

CP-MOD-041 moves the optional `JourneyVideoPrelude` class itself out of eagerly parsed `app.js`. Load the module only after explicit Lobby video-introduction selection or Settings audio-preview activation; deduplicate concurrent requests and clear the pending promise on failure. The service worker precaches the module without executing it, retaining offline controller code while the large video remains uncached and follows its safe unavailable fallback. Preserve all readiness/buffer/rebuffer timing, media/audio fades, DND acknowledgement, restart bypass, validation-before-video, and the normal journey dispatcher on module or media failure. Acceptance: initial browser load has no module request or global controller; explicit opt-in requests the module once then media; Settings preview also loads on demand; offline cache includes the controller; unit/source contracts, all journey tests and cold/warm/offline browser checks pass. This defers optional code parsing; do not claim a CPU, memory, thermal, or overall startup performance gain without measurement.

CP-MOD-042 moves the existing ~670-line `AmbientParticleField` class unchanged from `app.js` to `modules/ambient-particle-field.js`, loaded eagerly after its astronomy/night-sky dependencies and before the app. Dependency map: the owner calls `NaturalNightSky`, `SkyAstronomy`, and the app-provided `state.displayLanguage`, `CELESTIAL_LABEL_KEYS`, and `t`; it owns its canvas/context, observer fallback/geolocation, location label, cached celestial layer, Moon buffer, animation/timer/observer lifecycles, reduced-motion/static-screen guards, planetary/star placements, horizon, Earth illustration/five atmospheric layers, Sun shield, moonlight guide, black-hole illustration, meteors and cleanup. The app retains one shared `particleField` instance and existing call sites. Preserve pixel/render and observer behavior exactly; keep its exact module URL in the service-worker shell. Update the existing source-extraction contracts to target the new owner; validate all sky/unit tests, cold/warm/offline shell cache, and `tests/night-sky-browser.mjs` at desktop/mobile/daylight/reduced-motion/journey contexts. This is a maintainability boundary only: one extra eager request and no demonstrated startup, memory, CPU, or thermal gain.

CP-MOD-043 moves the small `VisualEngine` class unchanged from `app.js` to `modules/visual-engine.js`, loaded eagerly before the app. It continues to read the same app-owned visual preference state and normalization helper, constructs `CelestialPresence` with the same chakra DOM elements, and receives the same `AudioEngine` instance. All chakra effect class toggles, Eyes Close suppression, randomized image-breath cycle and glow reset remain unchanged. Keep CSS ownership, visual settings normalization/persistence and sky renderer in their existing owners. Update the direct source contracts and app wiring assertion, precache the versioned module, and rerun visual-effect, preference hydration, screenshot-independent route/unit and atlas checks. This is a small organization/testability extraction only; no visual or performance claim.

CP-MOD-044 moves the existing exact standard chakra-journey duration formula from `MeditationController` into the already eager/offline-cached `modules/session-estimate.js`. Inject scripts, selected chakra order, state, localization, timing, narration-duration, drone-duration, DOM reads and system narration explicitly. Keep the controller method as a compatibility delegate so the countdown selection/fallback branches stay fixed. Direct module tests must cover missing/demo/high-energy paths, zero selected chakras, narration/timing, selected add-on durations and selection-dependent duration. This is ownership/testability work only; do not claim startup, memory, CPU or thermal benefit.

CP-MOD-045 moves the exact session countdown-duration resolver from `MeditationController` into the same `modules/session-estimate.js` owner. Keep timer start/pause/stop lifecycle in the controller, supply the current selected-mode state and focused experience through explicit dependencies, and preserve mode precedence and duration formulas for Music Only, Box, preparation add-ons, Yoga, Intimate Service, Sleep, measured standard narration, HRIM, demo and legacy fallbacks. Cover each path in direct tests and synchronize the journey-addons and modularization maps. This is still eager ownership/testability work, not a startup or thermal optimization.

CP-MOD-046 extracts the journey countdown ticker lifecycle into `modules/session-countdown.js`: start/reset, the current 250 ms tick, inactive/paused elapsed-time handling, zero clamping, render and stop/cleanup. Inject the clock, timer APIs, active/paused state and display callbacks. Keep controller start/stop entry points unchanged and load/cache the module eagerly because every journey uses it. Deterministic tests must preserve restart, pause/resume, inactive, invalid-duration and cleanup behavior. This is an ownership/testability boundary; do not claim lower CPU or heat.

CP-MOD-047 extracts newcomer chakra-marker responsive geometry, coalesced animation-frame scheduling, image-load/ResizeObserver triggers and disposal into `modules/newcomer-marker-layout.js`. Preserve the seven normalized image anchors, viewBox calculation, connector endpoint/control-point formulas, eager/offline script order and the newcomer tutorial's existing screen/audio order. Direct injected tests cover left/right labels, incomplete/missing geometry, resize/load coalescing, cancellation and listener/observer cleanup. This is ownership/testability work only; it does not alter the user-facing flow or claim visual/performance gains.

CP-MOD-011/012 use identical local Chromium measurements as indicative comparisons, not production wire-size or device evidence: the local server does not compress responses and the harness blocks Google Fonts. CP-MOD-012 adds one eagerly loaded practice module; sampled response bodies grow about 1.4 KiB net, while timing deltas remain within single-run noise. This extraction is not a performance optimization. Select lazy boundaries only after extraction parity and repeatable cold/warm/offline comparisons.

After this track and the assessment are complete, proceed to the separate approved Cosmic Observatory visual redesign, CP-THEME-PLAN-001. Its reference and behavior-preservation contract live in `.loop/tracks/cosmic-observatory-theme/`; it does not alter the extraction sequence above.
